// Export all TypeScript types from this directory
// Example: export type { User, AuthState } from './auth';

export type {
    ProtectedRouteProps,
    UserRole,
    UserWithRole,
} from './auth.types';

export { };
